Dockerfile is to create a docker image of CentOS configured with tomcat on port 5000.
All other dependencies are alss installed. 

A docker image is created using this file and registered with Docker Hub https://hub.docker.com/r/tony8mt/tomcat5000/
